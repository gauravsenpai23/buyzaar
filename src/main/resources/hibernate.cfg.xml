<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- JDBC connection settings -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/buyzaar?useSSL=false&amp;serverTimezone=UTC</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">root</property>

        <!-- SQL dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Drop and re-create the database schema on startup -->
        <!-- Use `update` or `validate` in production -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Specify annotated classes or mapping files -->
        <!-- Example of annotated class -->
<!--        <mapping class="com.example.model.YourEntityClass" />-->

        <!-- You can also use mapping files like this -->
         <mapping class="entities.Category"/>
         <mapping class="entities.Product"/>
         <mapping class="entities.User"/>

    </session-factory>
</hibernate-configuration>
